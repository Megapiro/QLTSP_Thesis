"""
This class should contain all the methods required to preprocess the dataset.
It has been removed due to the proprietary structure of the dataset.
Once the dataset is prepared, we can encode it inside a model.
"""

class Preprocessing(object):
	pass